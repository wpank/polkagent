[package]
name = "polkagent-grant"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Policy evaluation, grant resolution, and gate system for the Polkagent platform"

[dependencies]
# Internal
polkagent-core = { path = "../polkagent-core" }
polkagent-identity = { path = "../polkagent-identity", optional = true }

# Async
async-trait = { workspace = true }
tokio = { workspace = true }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }

# Time
chrono = { workspace = true }

# Observability
tracing = { workspace = true }

# Errors
thiserror = { workspace = true }

# Crypto (used for deterministic principal-to-AgentId mapping)
blake3 = { workspace = true }

# IDs
uuid = { workspace = true }

[features]
identity = ["dep:polkagent-identity"]
personhood = ["identity"]

[dev-dependencies]
criterion = { workspace = true }
proptest = { workspace = true }
tempfile = { workspace = true }
tokio = { workspace = true, features = ["full", "test-util"] }
polkagent-identity = { path = "../polkagent-identity" }

[[bench]]
name = "policy"
harness = false
