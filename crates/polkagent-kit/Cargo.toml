[package]
name = "polkagent-kit"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Product kit manifest parsing, validation, install/uninstall lifecycle for the Polkagent platform"

[dependencies]
# Serialization
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }

# Errors
thiserror = { workspace = true }

# Semver
semver = { workspace = true }

# Observability
tracing = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
